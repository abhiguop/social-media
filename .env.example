# Backend-only environment variables

# App
PORT=4000
NODE_ENV=development

# MongoDB connection string
# For Docker Compose, use the service name 'mongo' as host
MONGO_URI=mongodb://mongo:27017/socialmedia

# For local development (uncomment and use instead of above)
# MONGO_URI=mongodb://127.0.0.1:27017/socialmedia

# JWT Secret
JWT_SECRET=change_me_super_secret

# SMTP (Mailtrap or other SMTP provider)
SMPT_HOST=sandbox.smtp.mailtrap.io
SMPT_PORT=2525
SMPT_USER=
SMPT_PASSWORD=
